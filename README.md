# Run main.py

Controls:

- `space`: start/stop simulation
- `q/escape`: quit program
- `s`: toggle display of sensors FOV
- `+/-`: change window size
- `ENTER`: make single simulation step (only if stopped)